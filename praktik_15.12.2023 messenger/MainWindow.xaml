<Window x:Class="praktik_15._12._2023_messenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:praktik_15._12._2023_messenger"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.Resources>
        <local:SenderToBackgroundConverter x:Key="SenderToBackgroundConverter" />
    </Window.Resources>-->
    <Grid>
        
        <!--<StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
            
        </StackPanel>-->
        <ListBox x:Name="ChatListBox" Grid.Row="0" ItemsSource="{Binding Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5" Padding="10" BorderBrush="Black" BorderThickness="1">
                        <TextBlock Text="{Binding Message}" />
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalAlignment" Value="{Binding HorizontalAlignment}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

    </Grid>
</Window>
